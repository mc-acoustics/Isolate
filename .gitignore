# Python
.venv/
__pycache__/
*.pyc

# PyInstaller (gerados pelo build.bat)
build/
dist/
Isolate.spec

# Runtime / locais
isolate.log
.claude/

# Diário de desenvolvimento — NUNCA publicar (regra do usuário, 09/07/2026)
CLAUDE.md

# Especificação original — local (removida do repo e do histórico em
# 17/08/2026 a pedido do usuário)
SPEC.md

# Material de divulgação (local)
promo/

# Reports de usuários (local — contém usernames/prints)
reports/

# Zips de release (vão para GitHub Releases, nunca para o repo — >100 MB)
Isolate-v*-win64.zip

# Fontes draw.io dos diagramas (locais por decisão de 17/07/2026)
docs/*.drawio
